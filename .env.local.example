# Copy this file to .env.local and fill in real values. .env.local is gitignored.

# Local Postgres connection string. Matches docker-compose.yml's `db` service by default
# (run `docker compose up -d db` first). Required.
DATABASE_URL="postgresql://newinvoice:newinvoice@localhost:5432/newinvoice?schema=public"

# Where `pnpm test` runs its integration tests (they create and delete rows, so this must
# never point at a database you care about). Optional — defaults to a `newinvoice_test`
# database on the same docker Postgres server, created and migrated automatically by the
# pretest hook. Uncomment only to override.
# TEST_DATABASE_URL="postgresql://newinvoice:newinvoice@localhost:5432/newinvoice_test?schema=public"

# Base URL the PDF adapter (Docs/implementation_decisions.md §12.2) navigates to when
# rendering /invoices/[id]/print. Required. Port 3001 is the app's default dev port
# (see package.json's dev script) — change both together if you need a different port.
NEXT_PUBLIC_APP_URL="http://localhost:3001"

# Which PDF rendering path to use. "local" (plain puppeteer, headless Chromium) is the
# development default; "serverless" (puppeteer-core + @sparticuz/chromium) exists for an
# eventual serverless deployment target and is not exercised in local development;
# "pdf-lib" (M32.1) draws the invoice directly from data in-process — no browser launch
# at all, sized for a memory-constrained deployment target. puppeteer/puppeteer-core/
# @sparticuz/chromium are devDependencies, so "local"/"serverless" only work when those
# are installed (always true in local dev; not guaranteed in a production-mode install).
PDF_ADAPTER="local"

# AI-assist chat panel (Docs/implementation_decisions.md §19) appears on every Party,
# Payment Method, and Invoice form (both creation AND editing an existing record), never
# on Project. It's pure progressive enhancement: with no provider configured, the panel
# shows a quiet "not configured" state and every form remains 100% usable by hand.
#
# Provider/model/API-key configuration is DB-backed — configure it at the in-app
# /settings page, not via env vars. SETTINGS_ENCRYPTION_KEY is the one AI-related env
# var: the local secret used to encrypt/decrypt saved API keys at rest (any string works,
# it's SHA-256-hashed to a key internally — just don't change it after keys are saved, or
# they become undecryptable). Required for /settings to function; the rest of the app
# works fine without it.
SETTINGS_ENCRYPTION_KEY=""

# M28 — authentication (Auth.js + Google OAuth, no self-registration; see
# Docs/implementation_decisions.md §8 and Docs/internal/feedback_backlog.md's
# M28 section for the full decided shape). No sign-in works at all without
# these three set.
#
# GOOGLE_CLIENT_ID/GOOGLE_CLIENT_SECRET come from a Google Cloud OAuth
# client you create yourself (console.cloud.google.com): consent screen
# audience "External", publishing status "In production" (our own `User`
# table is the real allowlist — Google's own "Testing" allowlist would just
# be redundant bookkeeping), authorized redirect URI
# http://localhost:3001/api/auth/callback/google for local dev (add your
# production URI once you have a real HTTPS domain — Google requires HTTPS
# for any non-localhost redirect URI).
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Signs/encrypts the Auth.js session cookie. Generate with
# `openssl rand -base64 32` or `npx auth secret`. Required.
AUTH_SECRET=""

# Production only (behind a reverse proxy) — not needed for local dev, where
# Auth.js resolves the host from the raw request with no proxy in front of it.
# AUTH_TRUST_HOST=true alone was not sufficient in practice (tested against
# next-auth 5.0.0-beta.32): the OAuth redirect_uri Auth.js sends to Google
# fell back to the app's internal bind address instead of the real public
# host, causing a redirect_uri_mismatch at Google's end. Setting AUTH_URL
# explicitly to the real public HTTPS origin bypasses that header-detection
# path entirely and is the reliable fix.
# AUTH_URL="https://your-production-domain"
# AUTH_TRUST_HOST=true
