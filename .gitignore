# Dependencies
node_modules/

# Next.js
.next/
out/

# Production build artifacts
build/
dist/

# Environment variables (never commit real secrets)
.env
.env.local
.env.*.local

# Prisma
prisma/generated/

# Testing
coverage/

# Logs
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor/OS
.DS_Store
*.pem
.vscode/*
!.vscode/extensions.json
.idea/

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Vercel
.vercel

/src/generated/prisma

# One-time real-invoice data-ingestion source material and the script that
# transcribed it (contains real bank account/SWIFT/ABA numbers in plaintext) —
# never to be preserved/committed.
/Data_ingestion
/scripts/ingestDataIngestionInvoices.ts
/scripts/ingest[redacted]G19.ts

# Pre-implementation reference material (stale specs, UI mockups, and real
# client invoice PDFs/screenshots used only as visual reference) — never
# to be committed. See CLAUDE.md's ai_context/ section.
/ai_context

# Local-only project docs: AI-session/process files and milestone/decision
# history tracked for Claude Code sessions (public-release decision,
# 2026-07-28). These contain session narrative and real client/business data
# and must never be committed — only current-state docs in Docs/ are public.
/Docs/internal
/CLAUDE.md
